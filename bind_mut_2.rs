// https://practice.course.rs/variables.html
// Fill the blanks in the code to make it compile
fn main() {
    let mut x: i32 = 1; // the mut keyword allows the var to be changed
    x += 2; 
    
    assert_eq!(x, 3);
    println!("Success!");
}